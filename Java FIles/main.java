public class main {
    public static void main(String[] args){
        Circle myCircle = new Circle();
        myCircle.setSize(10); // Set the size of the circle to 10 units
        double diameter = myCircle.getDiameter(); // Get the diameter (should be 10 units)
        double radius = myCircle.getRadius(); // Get the radius (should be 5 units)

        System.out.println(myCircle.getDiameter);
    }
}
